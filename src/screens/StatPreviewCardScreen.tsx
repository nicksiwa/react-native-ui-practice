import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';
import {Text} from 'react-native';

const StatPreviewCardScreen = () => {
  return (
    <SafeAreaView>
      <Text>Hello Stat Preview Card Component</Text>
    </SafeAreaView>
  );
};

export default StatPreviewCardScreen;
